#include "utils/marketmaker.h"
#include "utils/utils.h"
#include <iostream>
#include <chrono>
#include <thread>

using namespace std;

int main() 
{
    // reader 

    // ifstream stocks;
    // stocks.open(constants::STOCK_PATH);
    // ifstream options;
    // options.open(constants::OPTION_PATH);

    // string line;
    // while(getline(options,line))
    // {
    //     cout << line << endl;

    //     this_thread::sleep_for(chrono::milliseconds(constants::DELAY));
    // }
    return 1;
}
